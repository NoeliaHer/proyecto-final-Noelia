---
import Container from "@components/container.astro";
import Link from "@components/ui/link.astro";
import { Astronav, MenuItems, MenuIcon } from "astro-navbar";


const menuitems = [
{
    title: "Inicio",
    path: "/",
  },
  {
    title: "Campañas",
    path: "/blog",
  },
  {
    title: "Colaborar",
    path: "/colaborar",
  },
  {
    title: "Sobre mí",
    path: "/about",
  },
  {
    title: "Contacto",
    path: "/contact",
  },
];
---

<Container>
  <header class="flex flex-col lg:flex-row justify-between items-center bg-grey-100 p-4 shadow-md z-50">
    <Astronav>
      <div class="flex w-full lg:w-auto items-center justify-between">
        <a href="/" class="text-lg" style="display: flex; align-items: center;">
          <img src="/Adogta.jpg" alt="Logo AdogtaMascota" class="w-14 h-14 mr-3">
          <span class="font-bold text-slate-800">Adogta</span>
          <span class="text-slate-500">Mascota</span>
        </a>
        <div class="block lg:hidden">
          <MenuIcon class="w-4 h-4 text-gray-800" />
        </div>
      </div>
      <MenuItems class="hidden w-full lg:w-auto mt-2 lg:flex lg:mt-0">
          <ul class="flex flex-col lg:flex-row lg:gap-3">
            {menuitems.map(item => (
              <li>
                <a href={item.path} class={"flex lg:px-3 py-2 items-center text-gray-600 hover:text-black " + (item.title === "Colaborar" ? "bg-green-500 text-white rounded-md px-2 py-2" : "")}>
                  <span>{item.title}</span>
                </a>
              </li>
            ))}
          </ul>
          <div class="lg:hidden flex items-center mt-3 gap-4">
            <Link href="/login" class="link-acceder" style="muted" block size="md">Acceder</Link>
            <Link href="/register" size="md" block>Registrar</Link>
          </div>
        </MenuItems>
    </Astronav>
    <div>
      <div class="hidden lg:flex items-center gap-4">
        <a href="/login">Acceder</a>
        <Link href="/register" size="md">Registrar</Link>
      </div>
    </div>
  </div>
</header>
</Container>
